## Отчёт по лабораторной работе

Выполнили:

Студент (ФИО) | Роль в проекте   | Оценка
-------------|---------------------|------
Епифанов Евгений (312) | обучал RNN | 
Эсмедляев Фёдор (312) | обучал LSTM | 
Куценко Максим (312) | обучал двунаправленную LSTM | 
Салихов Тимур (312) | обучал GPT (с нуля) | 
Головенко Анатолий (312) | дообучал GPT | 

Для обучения использовался Google Colab с CUDA, а также локальная машина с mps.

1. RNN - импортируется как слой PyTorch. Используется посимвольная и пословная токенизация.
2. LSTM - импортируется как слой PyTorch. Используется посимвольная и пословная токенизация.
3. Двунаправленная LSTM - импортируется как слой PyTorch. Используется пословная токенизация.
4. Используется в основном код из статьи [GPT text generation from scratch with KerasHub](https://keras.io/examples/generative/text_generation_gpt/).
5. Из transformers импортируется GPT 2, дообучается на данных из датасета.